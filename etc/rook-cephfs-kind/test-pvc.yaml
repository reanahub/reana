apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rook-cephfs-smoke
  namespace: default
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: rook-cephfs
